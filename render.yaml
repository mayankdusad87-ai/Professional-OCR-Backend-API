services:
  - type: web
    name: professional-ocr-backend-api
    env: python
    buildCommand: |
      pip install -r requirements.txt
      python download_models.py
    startCommand: uvicorn main:app --host 0.0.0.0 --port 10000
